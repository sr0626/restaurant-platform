# 1. Create remote state bucket + DynamoDB lock (one-time)
aws s3 mb s3://swarasa-tfstate-sr0626 --region us-east-1
aws s3api put-bucket-versioning \
  --bucket swarasa-tfstate-sr0626 \
  --versioning-configuration Status=Enabled
aws dynamodb create-table \
  --table-name swarasa-tfstate-lock \
  --attribute-definitions AttributeName=LockID,AttributeType=S \
  --key-schema AttributeName=LockID,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST --region us-east-1

# 2. Create terraform.tfvars from the example
cp infra/terraform.tfvars.example infra/terraform.tfvars
# edit: set env=dev, add github_access_token


Then terraform init && terraform plan for a final review before apply.



github_access_token — create a GitHub Personal Access Token:

GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
Generate new token → scope: repo (full, needed for Amplify to clone + watch push events)
Copy it immediately — GitHub shows it only once
Set it: export TF_VAR_github_access_token="github_pat_..." (don't paste into the file)
allowed_origins — there's a chicken-and-egg problem here. The Amplify URL (https://main.{app_id}.amplifyapp.com) only exists after the first terraform apply. So do this in two passes:

Pass 1 — first apply:


allowed_origins = ["http://localhost:3000"]
After apply, run:


terraform output amplify_app_url
# → https://main.abc123xyz.amplifyapp.com
Pass 2 — update and re-apply:


allowed_origins = [
  "http://localhost:3000",
  "https://main.abc123xyz.amplifyapp.com"
]
This updates the API Gateway CORS config in ~30 seconds with no downtime.

One more thing — the github_access_token is stored in Terraform state (S3, encrypted at rest). That's acceptable for dev. For production, rotate it and use TF_VAR_github_access_token in CI/CD secrets rather than terraform.tfvars.



aws dynamodb delete-table \
  --table-name swarasa-tfstate-lock \
  --region us-east-1
If you also want to delete the S3 state bucket:


# Empty it first (S3 won't delete non-empty buckets)
aws s3 rm s3://swarasa-tfstate-sr0626 --recursive
aws s3 rb s3://swarasa-tfstate-sr0626